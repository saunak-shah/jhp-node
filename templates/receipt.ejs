<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Exam Receipt</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 40px;
      background-color: #f9f9f9;
      color: #333;
    }
    h2 {
      text-align: center;
      color: #2c3e50;
    }
    .student-info {
      display: flex;
      justify-content: space-between;
      margin-bottom: 30px;
      background-color: #ecf0f1;
      padding: 20px;
      border-radius: 8px;
    }
    .column {
      width: 48%;
    }
    .info-label {
      font-weight: bold;
    }
    .info p {
      margin: 4px 0;
    }
    .qr {
      margin-top: 20px;
      text-align: center;
    }

    .qr img {
      width: 100px;
      height: 100px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 20px 0;
      background: #fff;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    th, td {
      padding: 12px 15px;
      border: 1px solid #ddd;
    }
    th {
      background-color: #2c3e50;
      color: white;
      text-align: left;
    }
    .footer {
      margin-top: 40px;
      text-align: center;
      font-size: 0.9em;
      color: #777;
    }
  </style>
</head>
<body>
  <h2>Exam Receipt</h2>

  <!-- ✅ Student Info in Two Columns -->
  <div class="student-info">
    <div class="column">
      <p><span class="info-label">Name:</span> <%= student.first_name %> <%= student.last_name %></p>
      <p><span class="info-label">Father's Name:</span> <%= student.father_name %></p>
      <p><span class="info-label">Register No:</span> <%= student.register_no %></p>
      <p><span class="info-label">Email:</span> <%= student.email %></p>
    </div>
    <div class="column">
      <p><span class="info-label">Username:</span> <%= student.username %></p>
      <p><span class="info-label">Phone:</span> <%= student.phone_number %></p>
      <p><span class="info-label">Address:</span> <%= student.address %></p>
      <p><span class="info-label">Reg. ID:</span> <%= reg_id %></p>
    </div>
  </div>

  <table>
    <tr>
      <th>Course Name</th>
      <th>Location</th>
      <th>Start Time</th>
      <th>End Time</th>
      <th>Total Marks</th>
      <th>Passing Score</th>
    </tr>
    <tr>
      <td><%= course.course_name %></td>
      <td><%= location %></td>
      <td><%= new Date(start_time).toLocaleString() %></td>
      <td><%= new Date(end_time).toLocaleString() %></td>
      <td><%= total_marks %></td>
      <td><%= passing_score %></td>
    </tr>
  </table>

  <!-- <div class="qr">
    <p><strong>Registration ID:</strong> <%= reg_id %></p>
    <img src="data:image/png;base64,<%= qrCodeBase64 %>" alt="QR Code" />
  </div> -->

  <div class="footer">
    <p>Powered By: JHP Parivar © <%= new Date(created_at).getFullYear() %></p>
  </div>  
</body>
</html>
